let mysql;_26f‍.x([["connection",()=>connection]]);_26f‍.w("mysql",[["default",["mysql"],function(v){mysql=v}]]);

// Create a connection pool
const connection = mysql.createConnection({
  host: 'localhost',         // Your MySQL server's hostname
  user: 'root',      // Your MySQL username
//   password: 'yourpassword',  // Your MySQL password
  database: 'employee_log_manager'   // Your MySQL database name
});

// Try to connect to the database
connection.connect((err) => {
  if (err) {
    _26f‍.g.console.error('Error connecting to database:', err);
    return;
  }
  console.log('Connected to MySQL!');
//   startApp();
});

